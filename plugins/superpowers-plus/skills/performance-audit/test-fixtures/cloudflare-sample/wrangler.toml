# Dashboard-API Worker for a power-utility customer portal (binds D1 + KV + a Durable Object).
# Eval fixture for the performance-audit skill (illustrative; NOT deployed — lives under test-fixtures/).
# Answer key in expected-findings.md — assessor-only; do not read it when auditing this fixture.
name = "portal-api"
main = "index.js"
compatibility_date = "2025-01-01"

[[d1_databases]]
binding = "DB"
database_name = "portal"
database_id = "xxxx"

[[kv_namespaces]]
binding = "KV"
id = "yyyy"

[[durable_objects.bindings]]
name = "RATE_LIMITER"
class_name = "RateLimiter"
